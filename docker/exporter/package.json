{
  "name": "@auction/chain-exporter",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Standalone Prometheus exporter for the auction chain. Deliberately NOT an npm workspace member: it is built from its own lock file so a change to the root workspace never invalidates its image layer, and so it can be reasoned about (and audited) on its own.",
  "license": "MIT",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node src/main.mjs"
  },
  "dependencies": {
    "prom-client": "^15.1.3",
    "viem": "^2.21.0"
  }
}
